# Frontend environment variables for Vite
# Copy this file to .env and fill in your values.
# Note: VITE_* variables are exposed to the browser at build time.

# Supabase (used by src/integrations/supabase/client.ts)
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-public-anon-key

# Optional: externalize the Admin API base URL (used in src/pages/Admin.tsx)
VITE_API_BASE_URL=https://lookingcom-backend.vercel.app

# Server-side only (not used by Vite build):
# ANTHROPIC_API_KEY and MCP_URL are used by the Supabase Edge Function "chat".
# Set them as Supabase Function secrets in production:
#   supabase secrets set ANTHROPIC_API_KEY=sk-... MCP_URL=https://your-mcp-server.example.com/mcp/capcorn
# For local functions dev, create supabase/functions/.env with:
#   ANTHROPIC_API_KEY=sk-...
#   MCP_URL=https://your-mcp-server.example.com/mcp/capcorn
# The function reads them via Deno.env.get(...) in supabase/functions/chat/index.ts
# Do not prefix server-only secrets with VITE_
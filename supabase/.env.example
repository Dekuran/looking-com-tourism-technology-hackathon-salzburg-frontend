# Environment variables for Supabase Functions (Deno runtime)
# Copy to supabase/.env and set values for local dev with `supabase start`/`supabase functions serve`

# Anthropic API key for the chat function
ANTHROPIC_API_KEY=